{
  "name": "ejercicio3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "git push heroku main",
    "build:ui": "rm -rf build && cd ../../part2/ejercicio2.10 && npm run build && cp -r build ../../part3/ejercicio3/",
    "deploy:full": " npm run build:ui && git add . && git commit -m buildui && npm run deploy"
  },
  "author": "Federico",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
